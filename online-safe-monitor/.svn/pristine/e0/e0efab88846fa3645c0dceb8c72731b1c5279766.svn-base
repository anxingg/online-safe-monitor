 <decision name="${name}" >
   <#list paths as path>
      		<transition name="${path.text.text}" to="${path.to}" >
      			<condition expr="${path.expr}"/>
      		</transition>
      </#list>
 </decision>